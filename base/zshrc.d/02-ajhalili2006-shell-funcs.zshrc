#!/usr/bin/env zsh
# Bash-compartible shell functions and shortcuts, contributed from
# ~ajhalili2006's dotfiles repo, mostly are Git-related
alias stats="git status" status=stats
alias commit="git commit --signoff"
alias stage="git add"
alias unstage="git restire --cached"
alias switch-branch="git switch -c"
